# 🏢 앱 통제실 (App Layer)

프로젝트가 시작될 때 가장 먼저 실행되는 **전역(Global) 설정과 초기화**를 담당하는 곳입니다. 
C++이나 Java의 `main()` 함수가 있는 진입점이라고 생각하면 됩니다.

**무엇이 들어가나요?**
- 화면 라우팅 설정 (어떤 주소로 접속하면 어떤 페이지를 띄울지 연결)
- 프로젝트 전체에 적용되는 글로벌 CSS (기본 배경색, 여백 초기화 등)
- 전역 상태 관리 세팅 및 공통 프로바이더(Provider) 연결
