# =========================================
# 1. AI & AGENTIC WORKFLOWS (3_data_ai_pipeline)
# =========================================
langchain>=0.3.0
langchain-community>=0.3.0
openai>=1.55.0 
anthropic>=0.40.0
pydantic>=2.9.0
python-dotenv>=1.0.1

# High-performance inference for Qwen 2.5 32B (Server Only)
vllm>=0.6.3 
# Required for Qwen's attention mechanism efficiency (Server Only)
flash-attn>=2.6.3 

# =========================================
# 2. DATA SCIENCE & MACHINE LEARNING
# =========================================
numpy>=1.26.0  # Synced with local for data consistency
pandas>=2.2.0
torch>=2.5.0   # Optimized stable version for Python 3.12
transformers>=4.46.0
accelerate>=1.0.0
scikit-learn>=1.5.0

# =========================================
# 3. AUTOMATION & AUDITING (4_automation_tests)
# =========================================
playwright>=1.48.0
pytest>=8.3.0
pytest-playwright>=0.5.0
# Resolved: Using the version that fits Python 3.12
lighthouse-python==0.10.1

# =========================================
# 4. INFRASTRUCTURE & UTILITIES
# =========================================
requests>=2.32.0
aiohttp>=3.10.0
tqdm>=4.66.0
psutil>=5.9.0
clickhouse-driver>=0.2.9
psycopg2-binary>=2.9.10
redis>=5.1.0
rich>=13.9.0